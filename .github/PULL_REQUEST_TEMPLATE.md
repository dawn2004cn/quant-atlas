## What

Brief description of the change.

## Why

Link to issue or rationale.

## How

Key implementation approach.

## Checklist

- [ ] `py_compile` passes for all modified Python files
- [ ] Frontend builds (`npm run build` in `frontend/`)
- [ ] New/changed routes tested manually or via pytest
- [ ] OpenAPI spec updated if new endpoints added (`python scripts/generate_openapi.py`)
- [ ] No hardcoded secrets or IPs introduced
