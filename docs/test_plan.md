你是一位资深的移动端/后端架构师，精通Clean Architecture、领域驱动设计（DDD）和设计模式。

请对以下app代码进行全面重构分析：

分析维度要求（必须严格按此顺序输出）：

1. **当前架构评估**
   - 整体分层情况（是否符合分层架构原则：Presentation / Application / Domain / Infrastructure）
   - 各层职责是否清晰？是否存在跨层依赖、God Class、God Module？
   - 数据流向是否单向？依赖倒置原则执行情况如何？

2. **设计原则违反情况（逐条分析）**
   使用以下原则逐一检查（列出具体代码位置和问题）：
   - **单一职责原则 (SRP)**
   - **开闭原则 (OCP)**
   - **里氏替换原则 (LSP)**
   - **接口隔离原则 (ISP)**
   - **依赖倒置原则 (DIP)**
   - **迪米特法则 (LoD，最少知识原则)**  ← 这就是你说的“六原则”

3. **其他重要原则**
   - DRY / KISS / YAGNI
   - 关注点分离（Separation of Concerns）
   - 错误处理、一致性、可测试性

4. **重构建议（分优先级）**
   - **高优先级**（立即修复的架构问题）
   - **中优先级**（显著提升可维护性）
   - **低优先级**（优化空间）
   每条建议需包含：
     - 具体问题
     - 重构目标模式（例如Strategy、Factory、Repository、UseCase、Mediator等）
     - 重构后预期收益（可读性、可扩展性、可测试性等）
     - 迁移难度评估

5. **重构路线图**
   - 分阶段实施步骤（建议3-5个迭代）
   - 每个阶段的增量改动范围
   - 回归测试建议

6. **重构后的代码示例**
   - 提供2-3个关键类的重构前后对比（重点类优先）

请使用中文回复，代码使用Markdown格式。优先考虑实际项目可落地性，而非理论完美。