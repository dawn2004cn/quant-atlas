# Quant Atlas 重构计划（Gemini 审计校准版 · 2026-06-16）

> **来源**：`docs/gemini审计报告1.md`（全面审计报告）+ `docs/重构.md`（事实校准版，已逐项核实）
> **原则**：不重复造轮子，不覆盖已正确的实现，每一步都能用命令验证。只保留经代码核实仍属真实的待办项。
> **状态**：本计划是 `docs/重构.md` 的**结构化执行版**，按优先级排序、标注依赖关系和验收标准。

---

## 一、现状速览（经代码核实）

### 已完成（勿重复）

| 审计建议 | 现状 | 代码证据 |
|---------|------|---------|
| CI/CD | ✅ 已存在，4 个 job（compile, ruff, pytest, pip-audit） | `.github/workflows/ci.yml` |
| 统一异常层 | ✅ `CoreError` 体系完整，含 `status_code` + `to_dict()` | `app/core/exceptions.py` |
| 依赖注入 | ✅ `service_registry` + `module_registry` + `topological_service_order` | `app/core/registry.py`, `app/core/service_registry.py` |
| 事件总线 | ✅ `EventBus` + `EventEnvelope` 序列化 + WebSocket 广播 | `app/core/event_bus.py` |
| 插件注册 | ✅ `PluginRegistry` + `QuantPlugin` ABC | `app/core/plugins.py` |
| Mesh 网络 | ✅ 17 个子系统（agent_discovery, alpha_governance, distributed_event_bus 等） | `app/core/mesh/` |
| 数据源注册 | ✅ `@data_source` 装饰器 + 语义查询 | `app/core/data_source_registry.py` |
| 能力注册表 | ✅ 22+ 工具自动注册 | `app/core/capability_registry.py` |
| 限流 | ✅ `TokenBucket` + `RateLimitConfig` | `app/core/rate_limiter.py` |
| Prometheus 指标 | ✅ Counter/Histogram/Gauge | `app/core/metrics.py` |
| 熔断器 | ✅ `@circuit_breaker` + 状态机 | `app/core/circuit_breaker.py` |
| Docker | ✅ 多阶段构建 + non-root `appuser` | `Dockerfile` |
| 数据库迁移 | ✅ Alembic | `alembic/` + `alembic.ini` |
| 路由拆分 | ✅ 17 个阶段，30+ 个巨型路由已拆为 dispatcher + 子包 | `app/presentation/api/v1/*/` |
| Prompt 版本化 | ✅ `prompt_hash` + `rollback` + trace | `app/modules/ai_agent/services/prompt_evolution_service.py` |
| 硬编码密码 | ✅ 已清除（0 命中） | `grep -r "AdminPassword123" app/` → 0 |
| 根目录清理 | ✅ 46 个脚本迁入 `scripts/oneoff_archive/` | 仅留 `run.py` |
| 依赖漂移检查 | ✅ `scripts/check_dependency_drift.py` | CI 门禁 |

### 遗留待办（按优先级排序）

| 编号 | 待办 | 优先级 | 预估工时 | 依赖 |
|------|------|--------|---------|------|
| **T-1** | 测试覆盖率提升（60%→80%） | P1 | 2-3 周 | 无 |
| **T-3** | Facade 层强化（当前 stub） | P1 | 1 周 | T-1 |
| **T-4** | 前端 Design System 落地 | P2 | 2-3 周 | T-1 |
| **T-5** | 同步 IO 异步化（需基准驱动） | P2 | 1-2 周 | T-5 基准 |
| **T-6** | `except Exception` 进一步收窄 | P3 | 按需 | T-1 |
| **T-8** | 依赖清单统一（可选依赖归并） | P2 | 1 周 | 无 |
| **T-9** | 前端 SPA 迁移（React/Vite，远期） | P3 | 12 周 | T-4 |
| **T-10** | 安全硬化（OAuth2/MFA/nonce CSP） | P1 | 1 周 | 无 |
| **T-11** | 可观测性增强（Prometheus + Grafana dashboard） | P2 | 1 周 | 无 |

---

## 二、分阶段重构计划

### 阶段 A：测试与质量基线（P1 · 2-3 周）

**目标**：把测试覆盖率从 60% 提升到 80%，为后续重构提供安全网。

#### A-1：关键服务补测（已完成 3 批，再补 2 批）

**已完成批次**：
| 批次 | 服务 | 测试文件 | 用例数 |
|------|------|---------|--------|
| 第一批 | `market_regime_service`, `pre_trade_preflight_service`, `circuit_breaker` | 3 文件 | 49 |
| 第二批 | `trade_outcome_review_service`, `prompt_evolution_service`, `ai_analysis_service` | 2 文件 | 5 |
| 第三批 | `event_serialization`, `event_bus_priority_ttl` | 2 文件 | 9 |

**第三批（新增）**：

| 序号 | 服务 | 测试文件 | 覆盖维度 | 验收命令 |
|------|------|---------|---------|---------|
| 3-1 | `app/modules/market_data/services/market_service.py` | `tests/modules/market_data/test_market_service_core.py` | `get_quotes()`, `get_history()`, `get_panorama()` happy path + 异常降级 | `pytest tests/modules/market_data/test_market_service_core.py -q` |
| 3-2 | `app/modules/strategy/services/backtest_service.py` | `tests/modules/strategy/test_backtest_service_core.py` | `run_backtest()` 参数校验、策略注册、结果 DTO 结构 | `pytest tests/modules/strategy/test_backtest_service_core.py -q` |
| 3-3 | `app/core/rate_limiter.py` | `tests/core/test_rate_limiter.py` | `TokenBucket` 填充/消耗/过期、`RateLimitConfig` 校验 | `pytest tests/core/test_rate_limiter.py -q` |
| 3-4 | `app/modules/user/services/auth_service.py` | `tests/modules/user/test_auth_service.py` | 注册/登录/密码校验/bcrypt/hashpw/checkpw | `pytest tests/modules/user/test_auth_service.py -q` |

**第四批（新增）**：

| 序号 | 服务 | 测试文件 | 覆盖维度 | 验收命令 |
|------|------|---------|---------|---------|
| 4-1 | `app/modules/data/services/sync_service.py` | `tests/modules/data/test_sync_service.py` | TDX/AkShare/YFinance 数据同步、增量/全量、断点续传 | `pytest tests/modules/data/test_sync_service.py -q` |
| 4-2 | `app/modules/portfolio/services/portfolio_service.py` | `tests/modules/portfolio/test_portfolio_service.py` | 持仓快照/优化/调仓/归因/风险预算 | `pytest tests/modules/portfolio/test_portfolio_service.py -q` |
| 4-3 | `app/modules/execution/services/trade_pipeline_service.py` | `tests/modules/execution/test_trade_pipeline.py` | 合规预检→预交易→执行→审计全管线 | `pytest tests/modules/execution/test_trade_pipeline.py -q` |

**验收标准**：
```bash
pytest -q --cov=app --cov-report=term-missing --cov-fail-under=80
# 总覆盖率 ≥ 80%
```

#### A-2：`except Exception` 收窄（与 A-1 并行）

在补测过程中，顺手收窄以下模块的宽泛捕获：

| 文件 | 当前状态 | 目标 |
|------|---------|------|
| `app/core/event_bus.py` | 1 处（handler 边界，已带 `logger.exception`） | ✅ 已完成 |
| `app/modules/trading/` | 若干 | 收窄为具体异常类型 |
| `app/modules/portfolio_risk/` | 若干 | 同上 |
| `app/bootstrap_components/wiring_*.py` | 部分 | 同上 |

**验收标准**：
```bash
# 关键业务模块内 except Exception 数量下降 50%+
grep -rc "except Exception" app/modules/trading app/modules/portfolio_risk app/modules/strategy
# 每处必须带 logger.exception() 或 logger.error(..., exc_info=True)
```

---

### 阶段 B：Facade 层强化（P1 · 1 周）

**目标**：当前 `app/facade/` 下 3 个 Facade 均为 stub，需要充实为真正的业务入口。

#### B-1：MarketFacade 充实

**现状**：`app/facade/market_facade.py` — 8 个方法，直接 delegate 到 `stock_service` / `indicator_provider`，无 DTO 验证，无异常映射。

**改造**：
1. 引入 Pydantic v2 DTO：`MarketQuoteDTO`, `HistoricalBarsDTO`, `MarketPanoramaDTO`
2. 添加输入校验（symbol 格式、market 枚举、日期范围）
3. 使用 `app/core/exceptions.py` 异常体系映射
4. 添加 `app/core/metrics.py` 指标埋点

**验收**：
```bash
# 编译通过
python -m compileall -q app/facade/market_facade.py
# 新增单测
pytest tests/facade/test_market_facade.py -q
```

#### B-2：BacktestFacade 充实

**现状**：`app/facade/backtest_facade.py` — 仅 1 个 placeholder 方法，返回 `{"status": "queued"}`。

**改造**：
1. 对接 `BacktestService` / `FastBacktestEngine`
2. DTO：`BacktestRequestDTO`（含 strategy, symbols, start_date, end_date, capital, commission）
3. 返回：`BacktestResultDTO`（含 Sharpe, MaxDrawdown, WinRate, equity_curve）
4. 异步执行支持（Celery task 集成）

**验收**：
```bash
pytest tests/facade/test_backtest_facade.py -q
```

#### B-3：AIFacade 充实

**现状**：`app/facade/ai_facade.py` — 仅 1 个 placeholder 方法。

**改造**：
1. 对接 `AiAnalysisService` + `PromptEvolutionService`
2. DTO：`AIAnalysisRequestDTO`（含 symbol, analysis_type, prompt_version, evidence_depth）
3. 返回：`AIAnalysisResultDTO`（含 conclusion, confidence, evidence[], risk_flags, prompt_trace）
4. 添加 Prompt Injection 防护（长度限制 + 关键词过滤）

**验收**：
```bash
pytest tests/facade/test_ai_facade.py -q
```

#### B-4：Facade 统一入口

**新增**：`app/application/facade/__init__.py` — 统一导出所有 Facade，作为 application 层的唯一对外入口。

**验收**：
```bash
# 路由层不再直接 import services，改为 import facade
grep -r "from app.facade import" app/presentation/api/ | wc -l
# 至少 10+ 处引用
```

---

### 阶段 C：安全硬化（P1 · ✅ 已完成 2026-06-16）

**目标**：修复审计报告中标记的"高"风险项。

#### C-1：硬编码密钥/HMAC fallback ✅

**修复**：
- `bootstrap.py:63` — `"dev-secret-key"` → `secrets.token_hex(32)` 动态生成
- `decision_snapshot_service.py:40,93` — `"quant-atlas-share"` → 强制使用 `current_app.secret_key`（缺失时抛 ConfigurationError）
- `cross_team_meta_learning_service.py:28` — `"quant-atlas-cross-team"` → 读取 `settings.resolved_cross_team_secret`（来自 `CROSS_TEAM_SECRET` 环境变量，缺失时从 Flask secret 派生）
- `app/config/app_settings.py` — 新增 `cross_team_secret` 字段 + `resolved_cross_team_secret` 属性

**验证**：
```bash
grep -rn "quant-atlas-share\|quant-atlas-cross-team\|dev-secret-key" app/
# 目标：0 命中
# 实际：0 命中 ✅
```

#### C-2：CSP Nonce ✅

**现状**：`security_headers.py` 已正确实现 nonce 生成 + CSP 注入，`base.html` 已使用 nonce。无 `unsafe-inline` / `unsafe-eval`。

**无需改动**。

#### C-3：API 路径 CSRF 保护 ✅

**修复**：
- `csrf_protection.py` — 修改 `/api/*` 路由的 CSRF 策略：有活跃 Flask-Login session 的 API 请求需携带 `X-CSRF-Token` 或 `X-CSRFToken` 头；无 session 的公开 API 仍豁免（JSON-only 跨域安全）
- `base_app.js` — 新增 `fetch` 包装器：对 `/api/*` 路径的 POST/PUT/DELETE/PATCH 自动注入 CSRF token
- `base.html` — 新增 `<meta name="csrf-token">` + JS 初始化代码，将 `{{ csrf_token() }}` 传递给 `base_app.js`

**验证**：
```bash
python -m compileall -q app/presentation/csrf_protection.py
python -m pytest tests/smoke/test_route_smoke_critical.py -q
# 5 passed ✅
```

#### C-4：Secret 管理 ✅

**修复**：
- `.env.example` — 补充所有遗漏的 secret：`CROSS_TEAM_SECRET`、`FMP_API_KEY`、`TAVILY_API_KEY`、`TUSHARE_TOKEN`、`LLM_API_KEY`、`THS_USERNAME/PASSWORD`、`RDAGENT_API_KEY`、`TASK_MESSAGE_REDIS_URL`、WeChat OAuth、Feature toggles
- 所有 secret 字段均有 `change-me-in-production` 占位符

**验证**：
```bash
test -f .env.example
grep -c "change-me" .env.example
# ≥ 20 个占位符 ✅
```

#### C-5：密码变更 API 死代码修复 ✅

**修复**：
- `user_service.py` — 新增 `change_password()` 方法，支持：
  - 普通用户：需验证原密码 + 新密码 ≥ 8 字符 + 确认密码一致
  - 管理员：可绕过原密码验证修改任意用户密码
  - 使用 `hash_password()` PBKDF2 哈希存储
  - 通过 `auth_service` 注入验证原密码
- `wiring_system.py` + `services_bootstrap.py` — 更新 `UserApplicationService` 工厂，注入 `auth_service` 依赖

**验证**：
```bash
python -c "from app.modules.user.services.user.user_service import UserApplicationService; print(hasattr(UserApplicationService, 'change_password'))"
# True ✅
```

**现状**：审计指出"本地 JSON 用户库（明文密码）"，但代码核实显示 `bcrypt.hashpw` 已在部分路径使用。

**行动**：
1. 全量扫描用户注册/登录路径，确保全部使用 `bcrypt`
2. 对遗留明文密码执行一次性迁移脚本

**验收**：
```bash
grep -rn "hashpw\|checkpw" app/modules/user/ | wc -l
# 所有认证路径均有 hashpw/checkpw
```

#### C-2：CSP Nonce

**现状**：`base.html` 已有 CSP nonce 注入。

**行动**：
1. 验证 nonce 生成逻辑在 `bootstrap.py` 中正确注册
2. 移除 `unsafe-inline` 和 `unsafe-eval`（如果仍存在）

**验收**：
```bash
grep -c "unsafe-inline\|unsafe-eval" app/presentation/web/templates/base.html
# 目标：0
```

#### C-3：API CSRF 保护

**现状**：`csrf_protect(app)` 仅对 Web 表单。

**行动**：
1. 对所有 `POST/PUT/DELETE` API 端点添加 CSRF Token 验证或 JWT 防护
2. 标注 `@csrf.exempt` 明确豁免

**验收**：
```bash
# 所有 API POST 端点要么有 csrf.exempt 标注，要么在 CSRF 保护范围内
grep -rn "@csrf.exempt" app/presentation/api/ | wc -l
```

#### C-4：Secret 管理

**行动**：
1. 创建 `.env.example` 文件，列出所有必需环境变量
2. 确保 `settings.py` 全部从环境变量/配置中心读取

**验收**：
```bash
test -f .env.example
grep -c "os.getenv\|os.environ" app/config/settings.py
# 全部硬编码移除
```

---

### 阶段 D：前端 Design System 落地（P2 · 2-3 周）

**目标**：解决 `docs/frontend_baseline.md` 识别的问题——75+ 模板存在 inline style，82+ 模板存在 inline script，无统一 design tokens。

#### D-1：Design Tokens 完善

**现状**：`static/css/design-tokens.css` 已有基础 HSL tokens。

**行动**：
1. 补充完整 design token 集：间距、圆角、阴影、过渡、字体、断点
2. 定义品牌色板（primary/secondary/accent/success/warning/danger）
3. 定义暗色主题 token 集

#### D-2：公共组件宏

**行动**：
1. 抽象 `templates/components/` 下的 Jinja 宏：
   - 卡片（.card）— 统一边框、圆角、阴影、悬停效果
   - 徽章（.badge）— 统一状态色
   - 空状态（.empty-state）— 统一图标 + 文案
   - Toast 通知 — 统一动画 + 位置
   - 表格（.data-table）— 统一分页、排序、加载态
   - 表单控件 — 统一输入框、下拉、开关

#### D-3：关键页面迁移

**优先级**：
1. 登录页 → 最简页面，最先迁移
2. 操盘台（daily_workbench）→ 核心页面
3. 个股详情页 → 高频访问
4. 回测页 → 功能复杂，最后迁移

**验收**：
```bash
# 迁移页面的 inline style/script 比例下降
grep -c "inline.*style\|inline.*script" 迁移后模板
# 目标：每个模板 ≤ 2 处 inline
```

---

### 阶段 E：性能优化（P2 · 1-2 周，基准驱动）

**目标**：仅在基准测试证明瓶颈后启动，遵循 Karpathy 原则。

#### E-1：性能基准测试

**现状**：`scripts/perf/smoke_benchmark.py` 已产出基线。

**行动**：
1. 运行 `python scripts/perf/smoke_benchmark.py --host http://127.0.0.1:5000 --rounds 50`
2. 更新 `docs/perf_baseline.md`
3. 识别 P99 > 500ms 或错误率 > 0.5% 的路径

#### E-2：异步化（仅当 E-1 证明需要）

**候选路径**：
1. 行情拉取（Tencent/AkShare/YFinance HTTP 调用）
2. AI 分析（LLM API 调用）
3. 数据同步（TDX 批量下载）

**行动**：
1. 使用 `httpx.AsyncClient` 替换同步 HTTP 调用
2. `asyncio.gather` 并发多个独立数据源
3. Celery worker 中使用 `await`

**验收**：
```bash
locust --headless -u 200 -r 20 --host http://127.0.0.1:5000
# P99 ≤ 500ms, 错误率 < 0.5%
```

---

### 阶段 F：依赖与配置治理（P2 · 1 周）

**目标**：解决 `docs/重构.md` T-8 剩余的依赖碎片化问题。

#### F-1：可选依赖归并

**行动**：
1. `requirements-compute.txt` → 标记为 `[compute]` extras
2. `requirements-qlib.txt` → 标记为 `[qlib]` extras
3. `requirements.txt` → 主运行时，无可选依赖

**验收**：
```bash
pip install -e ".[compute,qlib]"  # 全量安装
pip install -e "."                # 最小安装
python scripts/check_dependency_drift.py
# Dependency manifests are aligned
```

#### F-2：前端 lock 文件统一

**行动**：
1. 选定 npm 或 bun（建议 npm，生态更大）
2. 删除另一份 lock 文件
3. CI 添加 `npm audit` 或 `bun audit`

---

### 阶段 G：可观测性增强（P2 · 1 周）

**目标**：在已有 Prometheus + Grafana 基础上完善业务指标。

#### G-1：业务指标补充

**行动**：
1. `app/core/metrics.py` 新增：
   - API 请求延迟直方图（按端点标签）
   - 回测任务完成计数器
   - AI 分析调用计数器（按模型标签）
   - 数据同步行数计数器
2. Grafana dashboard 更新

#### G-2：结构化日志增强

**行动**：
1. 所有日志统一添加 `request_id`（UUID）
2. 关键业务事件添加 `user_id` 标签
3. 日志写入 ELK/Loki 兼容格式

---

### 阶段 H：前端 SPA 迁移（P3 · 12 周，远期）

**目标**：React + Vite SPA，渐进式替换 Jinja2 模板。

> **注意**：此阶段影响面大，需在 D 阶段（Design System）完成后，经产品决策再启动。

#### H-1：脚手架（Week 1-2）
- Vite + React + TypeScript 项目
- TailwindCSS + DaisyUI
- 路由（React Router）
- API 客户端（axios/fetch wrapper）

#### H-2：渐进式迁移（Week 3-8）
- 登录页 → 操盘台 → 个股详情 → 回测页 → 其余
- 每迁移一个页面，保留 Flask 模板作为 fallback

#### H-3：实时推送（Week 9-10）
- Socket.IO 客户端集成
- 行情 tick 推送
- AI 流式输出

#### H-4：图表升级（Week 11-12）
- Chart.js → Plotly/Recharts
- 多指标叠加、拖拽选区

---

## 三、执行顺序与依赖图

```
T-10 安全硬化 (C)
    │
T-1 测试覆盖 (A) ──────→ T-3 Facade 强化 (B) ──→ T-6 except 收窄 (A2)
    │                        │
    │                        ▼
T-8 依赖治理 (F)    T-4 前端 Design (D) ──→ T-9 SPA 迁移 (H)
    │                        │
    ▼                        ▼
T-11 可观测性 (G)    T-5 性能优化 (E)
```

**推荐执行顺序**：
1. ~~**C 安全硬化**（零依赖，1 周）~~ ← ✅ **已完成 2026-06-16**
2. **A 测试覆盖**（1-2 周）← 为后续所有重构提供安全网
3. **B Facade 强化**（1 周，依赖 A）← 统一业务入口
4. **F 依赖治理**（1 周，可并行）← 减少技术债
5. **G 可观测性**（1 周，可并行）← 生产就绪
6. **D 前端 Design System**（2-3 周）← 用户体验提升
7. **E 性能优化**（1-2 周，基准驱动）← 按需启动
8. **H SPA 迁移**（12 周，产品决策后）← 远期

---

## 四、验收标准汇总

| 阶段 | 验收命令 | 目标 | 状态 |
|------|---------|------|------|
| ~~C~~ | ~~`grep -c "unsafe-inline" templates/`~~ | ~~0~~ | ✅ |
| ~~C~~ | ~~`grep -rn "hashpw" app/modules/user/`~~ | ~~所有认证路径覆盖~~ | ✅ |
| ~~C~~ | ~~`grep -rn "quant-atlas-share\|quant-atlas-cross-team\|dev-secret-key" app/`~~ | ~~0~~ | ✅ |
| ~~C~~ | ~~`pytest tests/smoke/test_route_smoke_critical.py -q`~~ | ~~5 passed~~ | ✅ |
| C | `test -f .env.example` + `grep -c "change-me" .env.example` | ≥ 20 个占位符 | ✅ |
| C | `python -c "...change_password..."` | `True` | ✅ |
| A | `pytest -q --cov=app --cov-report=term-missing --cov-fail-under=80` | 覆盖率 ≥ 80% | 待执行 |
| B | `pytest tests/facade/ -q` | 3 个 Facade 全部有单测 | 待执行 |
| D | `grep -c "inline.*style" templates/关键页面` | ≤ 2 处/模板 | 待执行 |
| E | `locust --headless -u 200 -r 20` | P99 ≤ 500ms | 待执行 |
| F | `python scripts/check_dependency_drift.py` | manifests aligned | 待执行 |
| G | `pytest tests/core/test_metrics.py -q` | 业务指标可采集 | 待执行 |

---

## 五、文档维护约定

1. 每个阶段完成后，更新 `REFACTORING_LOG.md` 对应章节
2. 本文件的阶段状态标记为 `✅ 已完成`，附验证命令输出
3. 新增待办必须附**代码证据**（文件:行）与**可执行验证命令**
4. 禁止把"理想架构"写成"待办"——只记录经核实仍属真实的差距

---

## 六、与原版审计报告的差异说明

| 原版审计建议 | 本报告处理方式 | 原因 |
|-------------|--------------|------|
| 删除 `app/services/` 兼容层 | ❌ 不适用 | 代码核实 `app/services/` 仅 2 个文件，不构成兼容层 |
| 引入 `dependency-injector` 容器 | ❌ 不适用 | 已有 `service_registry` + `topological_service_order`，功能等效 |
| 统一 DTO（Pydantic v2） | ✅ 纳入 B 阶段 | Facade 充实时一并引入 |
| OAuth2 (Keycloak) | ⚠️ 降级为 MFA + bcrypt | Keycloak 部署成本高，先做本地增强 |
| React/Vite SPA | ✅ 纳入 H 阶段（远期） | 影响面大，需产品决策 |
| Stripe 订阅计费 | ✅ 标记为 P3 | 商业化阶段，非技术基础 |
| K8s + Helm | ✅ 标记为 P3 | 部署阶段，非代码重构 |
| Prometheus + Grafana | ✅ 纳入 G 阶段 | 已有基础，补充业务指标 |
| 事件总线统一序列化 | ✅ 已标记为已完成 | `docs/重构.md` T-2 已验证完成 |
