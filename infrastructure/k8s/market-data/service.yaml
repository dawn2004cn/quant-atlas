apiVersion: v1
kind: Service
metadata:
  name: market-data
  namespace: quant-atlas
spec:
  selector:
    app: market-data
  ports:
    - port: 5101
      targetPort: 5101
      name: http
  type: ClusterIP
