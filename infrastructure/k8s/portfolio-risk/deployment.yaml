apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-risk
  namespace: quant-atlas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: portfolio-risk
  template:
    metadata:
      labels:
        app: portfolio-risk
    spec:
      containers:
        - name: portfolio-risk
          image: quant-atlas/portfolio-risk:latest
          ports:
            - containerPort: 5401
          env:
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: quant-atlas-config
                  key: REDIS_URL
            - name: MYSQL_URL
              valueFrom:
                configMapKeyRef:
                  name: quant-atlas-config
                  key: MYSQL_URL
            - name: SERVICE_PORT
              value: "5401"
          livenessProbe:
            httpGet:
              path: /health
              port: 5401
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 5401
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
